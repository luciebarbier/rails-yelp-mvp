<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %> - <%= link_to "Details", restaurant_path(restaurant)%></li>

  <% end %>
</ul>

<%= link_to "Add a new restaurant", new_restaurant_path %>

